# Fonts

## Firecode

```
Make sure that the universe (for Ubuntu) or contrib (for Debian) repository is enabled (see how-to for Ubuntu or Debian docs)
Install fonts-firacode package either by executing 

sudo apt install fonts-firacode


in the terminal or via GUI tool (like “Software Center”)
```